#include<bits/stdc++.h>
using namespace std;
pair<string,string> solve(){
    int n,sum;
    cin>>n>>>sum;
    if(sum<n) return {"-1","-1"};
    int no=(sum/n);
    int len=n;
    int rem=(sum%n);
    string s="";
    while(n--){
        s+=(no+'0');
    }
    while
}
int main(){
    int n,sum;
    cin>>n>>sum;
    string s="";
    int no=(sum/n);
    return 0;
}